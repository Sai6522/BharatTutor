services:
  - type: web
    name: bharat-tutor
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: SARVAM_API_KEY
        sync: false
      - key: PORT
        value: 3000
